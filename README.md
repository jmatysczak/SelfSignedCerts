### Create and use self signed certs

#### certs
Create a root CA cert and "server" and "client" certs that can be used to authenticate both ways.

#### Python
Example server and client implementations that use the certs created above.
